#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define rp(s, e, i) for (ll i = s; i < e; i++)
#define tc    \
    ll t;    \
    cin >> t; \
    while (t--)
#define mod 1e9 + 7

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    tc
    {
        
    }

    return 0;
}
